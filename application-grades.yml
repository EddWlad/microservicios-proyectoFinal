spring:
  application:
    name: CONFIG-DEV
  datasource:
    url: jdbc:postgresql://localhost:5435/db_donkey
    username: donkeyPg
    password: pwd1234567890
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
